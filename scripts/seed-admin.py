#!/usr/bin/env python3
"""
Create the first system admin account on a fresh install.
Reads ADMIN_EMAIL and ADMIN_PASSWORD from environment (or .env).
Usage: python3 scripts/seed-admin.py
Implemented in Phase 1 once the DB schema and auth service exist.
"""
import sys

print("seed-admin.py: Phase 1 deliverable — not yet implemented.")
print("Once Phase 1 is complete, this script will create the system admin from .env values.")
sys.exit(0)
